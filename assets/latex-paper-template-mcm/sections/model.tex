\section{Model Development}

Derive the primary model from the stated assumptions. For example, introduce a
decision vector \(x\), objective \(f(x)\), and feasible set \(\mathcal{F}\):
\[
  \min_{x\in\mathcal{F}} f(x).
\]
Connect every model component to data, estimation, or domain reasoning.

Before finalizing this section, explain the simpler parent model, the evidence
that justified each added mechanism, the roles of shared and nuisance
parameters, and the identifiability boundary. Insert the generated
model-reasoning and parameter-role tables when they carry evidence; do not copy
the internal audit verbatim.
